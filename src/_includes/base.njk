---
title: My default title
---
<!DOCTYPE html>
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{path}}assets/global.css"/>
    {% if sync_css %}
            <link rel="stylesheet" type="text/css" href="{{sync_css}}">
        {% endif %}
    {% if defer_js %}
      <script src="{{defer_js}}" defer></script>
    {% endif %}
    {% if module_js %}
      <script src="{{module_js}}" type="module"></script>
    {% endif %}
  </head>
  <body>
    <main>
        {% include "_header.njk" %}   
      {{ content | safe }}
      <small>A website by {{ site.author }} - &copy; {{ site.copyrightYear }}</small>
    </main>
  </body>
</html>